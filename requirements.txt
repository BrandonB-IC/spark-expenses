# Anthropic SDK — Claude vision OCR for receipts
anthropic>=0.40.0

# Google Drive API
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.1

# PDF & image handling (Pillow used by audit packet image handling; pdfplumber
# kept available for any direct PDF text extraction we may need outside vision)
pdfplumber>=0.10.0
Pillow>=10.0.0

# Web dashboard
flask>=3.0.0
markdown>=3.5

# NOT YET NEEDED (add back when these features land):
# openpyxl>=3.1.0    # Excel output — v1 uses CSV ledger via stdlib csv
# reportlab>=4.0.0   # PDF merging for audit packet — v1 dumps original PDFs to a folder

# Config / env
python-dotenv>=1.0.0

# HTTP for currency API
requests>=2.31.0
